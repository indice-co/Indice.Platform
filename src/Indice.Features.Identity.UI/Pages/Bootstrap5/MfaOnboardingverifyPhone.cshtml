@page "/login/mfa/onboarding/verify-phone"

@model BaseMfaOnboardingVerifyPhoneModel

@inject IWebHostEnvironment HostingEnvironment
@inject IdentityUILocalizer Localizer

@{
    ViewBag.Title = Localizer.MfaOnBoardingVerifyPhone_PageTitle;
    var infoMessage = TempData.Peek<ExtendedValidationTempDataModel>(BaseMfaOnboardingVerifyPhoneModel.TempDataKey);
    var displayNext = infoMessage != null && infoMessage.Alert?.AlertType == AlertType.Success && !string.IsNullOrEmpty(infoMessage.NextStepUrl);
}

<section class="auth-card mfa">
    <vc:page-heading title="@Localizer.MfaOnBoardingVerifyPhone_PageHeader" image-src="" />
    <partial name="_ValidationSummary" />
    <partial name="_Alert" model="infoMessage?.Alert" />
    <form asp-route-returnUrl="@Model.Input.ReturnUrl" method="post" novalidate class="auth-form">
        <input type="hidden" asp-for="Input.ReturnUrl" />
        <input type="hidden" asp-for="Input.PhoneNumber" />
        <div class="field">
            <label asp-for="Input.Code" class="field__label">@Localizer.MfaOnBoardingVerifyPhone_Code</label>
            <input class="form-control field__control otp-field__input" data-otp-length="6" type="text" inputmode="numeric" autocomplete="one-time-code" placeholder="@Localizer.MfaOnBoardingVerifyPhone_Code" asp-for="Input.Code" disabled="@displayNext" autofocus aria-describedby="Input_Code-error" />
            <span asp-validation-for="Input.Code" class="field__error" id="Input_Code-error"></span>
        </div>
        <div class="auth-form__actions @(displayNext ? "auth-form__actions--split" : string.Empty)">
            <button class="btn btn-primary auth-form__submit" type="submit" disabled="@displayNext">@Localizer.MfaOnBoardingVerifyPhone_Save</button>
            @if (displayNext)
            {
                <a class="btn btn-primary auth-form__submit" href="@infoMessage!.NextStepUrl">@Localizer.MfaOnBoardingVerifyPhone_Next</a>
            }
        </div>
    </form>
</section>

@section scripts {
    <partial name="_ValidationScripts" />
    <script type="text/javascript" src="~/js/otp-field.js" csp-nonce></script>
}
