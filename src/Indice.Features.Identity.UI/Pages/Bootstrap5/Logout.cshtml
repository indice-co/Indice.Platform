@page "/logout"

@model BaseLogoutModel
@inject IdentityUILocalizer Localizer

@{
    var title = ViewData["Title"] = Localizer.Logout_Logout;
}

<section class="identity-panel wide logout-page">
    <article class="text-center">
        <vc:page-heading title="@title" image-src="" />
        <p>@Localizer.Logout_Would_You_Like_To_Logout</p>
        <form asp-page="/Logout">
            <input type="hidden" name="logoutId" value="@Model.LogoutId" />
            <fieldset>
                <div class="mb-3">
                    <button class="btn btn-primary">@Localizer.Logout_Yes</button>
                </div>
            </fieldset>
        </form>
    </article>
</section>
