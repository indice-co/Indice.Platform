@model AlertModel

@if (!Model.IsEmpty())
{
    var type = $"{Model.AlertType}".ToLower();
    <div class="alert alert-@type" role="@(type == "danger" || type == "warning" ? "alert" : "status")">
        @Model.Message
    </div>
}
